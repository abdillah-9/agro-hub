{
  "name": "agro-hub-nodejs-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "migrate:latest": "knex migrate:latest --env development --knexfile knexfile.cjs",
    "migrate:rollback": "knex migrate:rollback --env development --knexfile knexfile.cjs",
    "migrate:rollbackall": "knex migrate:rollback --all --env development --knexfile knexfile.cjs",
    "migrate:make": "knex migrate:make --env development --knexfile knexfile.cjs",
    "seed:run": "knex seed:run --env development --knexfile knexfile.cjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "knex": "^3.1.0",
    "multer": "^2.0.2",
    "mysql2": "^3.15.0"
  }
}
